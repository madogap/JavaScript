const idade = prompt("Sua idade")

    if (idade > 18) {
        alert('voce é maior de idade');
    } else if (idade > 12) {
        alert('voce é menor');
    } else if (idade < 12) {
        alert("Voce é criança")
    } else {
        alert('.......')
    }


    //Operador ternário

     const res = (7===7) ? "Verdade" : "Falses";
    
     console.log(ReadableStreamBYOBReader);



        


    